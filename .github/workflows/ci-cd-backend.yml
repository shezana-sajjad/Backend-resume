name: CI/CD Backend

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r MyFunction/requirements.txt

    - name: Set environment variables
      run: |
        echo "AZURE_COSMOSDB_ENDPOINT=${{ secrets.AZURE_COSMOSDB_ENDPOINT  }}" >> $GITHUB_ENV
        echo "AZURE_COSMOSDB_KEY=${{ secrets.AZURE_COSMOSDB_KEY }}" >> $GITHUB_ENV

    - name: Run tests
      env:
        AZURE_COSMOSDB_ENDPOINT: ${{ secrets.AZURE_COSMOSDB_ENDPOINT }}
        AZURE_COSMOSDB_KEY: ${{ secrets.AZURE_COSMOSDB_KEY }}
      run: |
        cd MyFunction/test
        pytest

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS_STUDENT }}

    - name: Deploy ARM Template
      run: |
        az deployment group create --resource-group classshezana--template-file Infrastructure/merged-template.json --parameters Infrastructure/merged-parameters.json
